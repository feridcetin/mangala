<resources>
    <!-- Ana uygulamanın teması. -->
    <style name="Theme.Mangala" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Birincil renk paleti. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- İkincil renk paleti. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Durum çubuğu rengi. -->
        <item name="android:statusBarColor" targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Özelleştirilmiş Mangala Buton Stili -->
        <item name="buttonBarNegativeButtonStyle">@style/Widget.AppCompat.Button.Borderless</item>
        <item name="buttonBarPositiveButtonStyle">@style/Widget.AppCompat.Button.Colored</item>
    </style>

    <!-- Mangala Oyunu için Cep Butonu Stili -->
    <style name="MangalaPocketButton" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">60dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">30sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#E0E0E0</item>
        <item name="android:background">@drawable/pocket_background_oval</item>
    </style>
</resources>
